apiVersion: 1

providers:
  - name: ELH Dashboards
    orgId: 1
    folder: ELH
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
